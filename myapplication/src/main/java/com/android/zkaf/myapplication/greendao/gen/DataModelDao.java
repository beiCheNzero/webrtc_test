package com.android.zkaf.myapplication.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.android.zkaf.myapplication.main.model.DataModel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DATA_MODEL".
*/
public class DataModelDao extends AbstractDao<DataModel, Long> {

    public static final String TABLENAME = "DATA_MODEL";

    /**
     * Properties of entity DataModel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Target = new Property(1, String.class, "target", false, "TARGET");
        public final static Property LatestEventField = new Property(2, String.class, "latestEventField", false, "LATEST_EVENT_FIELD");
    }


    public DataModelDao(DaoConfig config) {
        super(config);
    }
    
    public DataModelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DATA_MODEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"TARGET\" TEXT," + // 1: target
                "\"LATEST_EVENT_FIELD\" TEXT);"); // 2: latestEventField
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DATA_MODEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, DataModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String target = entity.getTarget();
        if (target != null) {
            stmt.bindString(2, target);
        }
 
        String latestEventField = entity.getLatestEventField();
        if (latestEventField != null) {
            stmt.bindString(3, latestEventField);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, DataModel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String target = entity.getTarget();
        if (target != null) {
            stmt.bindString(2, target);
        }
 
        String latestEventField = entity.getLatestEventField();
        if (latestEventField != null) {
            stmt.bindString(3, latestEventField);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public DataModel readEntity(Cursor cursor, int offset) {
        DataModel entity = new DataModel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // target
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // latestEventField
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, DataModel entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTarget(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setLatestEventField(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(DataModel entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(DataModel entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(DataModel entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
